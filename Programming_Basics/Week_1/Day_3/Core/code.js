// this variable keeps track of the number of likes
var likeCount = 0;
// the function of this code block the repetation when i dont called
function increaseLikes() {
   // the like count by 1
    likeCount = likeCount + 1;
}
